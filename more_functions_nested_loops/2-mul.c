#include "main.h"
/**
 * mul - Entry point
 *
 * Description: 'Multiplying two number using mul function'
 * @a : parameter
 * @b:the parameter
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}

