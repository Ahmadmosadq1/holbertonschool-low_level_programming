#include "main.h"

/**
 * _abs - Entry point
 *
 * Description: 'the program prints absulute value
 * @int: describe the parameter
 *
 * Return: Always 0 (Success)
 */

int _abs(int num)
{
	if (num < 0)
	return (num * -1);
	else
	return (num);
	return (0);
}

